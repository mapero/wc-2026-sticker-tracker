# Origin of the self-hosted sync backend (see server/).
# Leave empty in dev — the Vite proxy forwards /api to http://localhost:8787.
# In production set this to the deployed server origin, e.g.
#   VITE_SYNC_URL=https://sync.example.com
VITE_SYNC_URL=
